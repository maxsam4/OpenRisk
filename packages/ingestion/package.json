{
  "name": "@dra/ingestion",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "ingest": "tsx src/cli.ts",
    "ingest:safe": "tsx src/cli-safe.ts"
  },
  "dependencies": { "@dra/core": "workspace:*", "yaml": "^2.5.0" },
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.19.0", "vitest": "^2.1.0", "typescript": "^5.6.0" }
}
